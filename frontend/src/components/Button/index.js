const Button = () => {
  return (
    <form>
      <div>
        <button>Yes</button>
      </div>
      <div>
        <button>Retake</button>
      </div>
    </form>
  );
};

export default Button;
